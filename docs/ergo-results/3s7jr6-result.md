commit 332d7daf7: never-compacted backrun-quarantine-resolved.jsonl beside the quarantined journal. Archive record (own v tag) appended at both resolve-tombstone sites (finality + operational rescue). Adds resolution_archive module surface: ArchivedResolution + ResolutionArchiveRecord + QuarantineJournal::record_resolution (warn-and-continue on I/O). 3 roundtrip tests: compaction-survives (byte-identical across repeated compacts), torn-tail tolerance, slot_taken by round-trip. settlement_bot expect_used exemption was gate-necessary (workspace deny, inherited from efa0cc9ff). Adversarial review by supervisor added the by round-trip test; bin hunks split-staged so the B5 registry hunks remain uncommitted for the architecture session close. Bisect note: b7435e861 and 332d7daf7 each fail cargo check standalone on the backrun_sidecar bin; the pending B5-bin commit heals HEAD.
