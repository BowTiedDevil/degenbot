//! The `AnvilFork` handle — lifecycle + `Provider` + dev-RPC surface.
//!
//! Not yet implemented (task R4JPXF — "Rust `AnvilFork` struct: lifecycle +
//! `Provider` + `AnvilApi`"). This is the scaffold placeholder; the struct,
//! its constructor, the `ForkError` enum, and the `AnvilApi`-backed dev
//! methods land in that task.
