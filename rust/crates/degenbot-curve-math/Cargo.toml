[package]
name = "degenbot-curve-math"
description = "Pure-Rust ports of Curve StableSwap invariant math."
version.workspace = true
edition.workspace = true
publish.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[lints.rust]
warnings = "deny"

[lints.clippy]
complexity = "warn"
correctness = "warn"
pedantic = "warn"
perf = "warn"
style = "warn"
suspicious = "warn"

[dependencies]
alloy = { version = "^2.0" }

[dev-dependencies]
proptest = "^1.10"
criterion = { version = "^0.8", default-features = false, features = ["cargo_bench_support"] }


[[bench]]
name = "stableswap"
harness = false
