[package]
name = "degenbot-solidly-math"
description = "Pure-Rust ports of Solidly / Aerodrome / Camelot stable-pool invariant math."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[lints]
workspace = true

[dependencies]
alloy = { version = "^2.4" }

[dev-dependencies]
proptest = "^1.11"
serde_json = "^1.0"
