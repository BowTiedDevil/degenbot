MAX_SWAP_DATA_LENGTH: constant(uint256) = 4096

@view
def token0() -> address: ...

@view
def token1() -> address: ...

@nonpayable
def swap(
    amount0Out: uint256,
    amount1Out: uint256,
    to: address,
    data: Bytes[MAX_SWAP_DATA_LENGTH]
): ...
