{
  "id": "0e6e0120",
  "title": "Update simulation pipeline for expected_balance and ERC6909 profit tracking",
  "tags": [
    "simulation",
    "profit-check",
    "expected-balance",
    "P0"
  ],
  "status": "complete",
  "created_at": "2026-06-13T23:09:12.440Z"
}

## Summary
DONE: Updated simulation pipeline for expected_balance.

### Changes Applied
- The 7-call simulation pattern remains the same (it correctly measures WETH+ETH+ERC6909 balance changes)
- execute() call now includes expected_balance parameter (using mode 0 — skip on-chain check)
- Pre-tx balance reads still measure off-chain profit correctly
- CmdExecutorComposer now encodes `execute(bytes,uint256)` with expected_balance=0

### Future Enhancement
- Option C from the todo: fetch pre-tx balances once per dispatch block and reuse across simulations
- Use check_mode=1 or 2 instead of 0 for on-chain profit assertion in production
