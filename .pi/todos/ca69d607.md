{
  "id": "ca69d607",
  "title": "ADR-006 Slice 9: Backrun example migration to the new topology (overall acceptance)",
  "tags": [
    "adr-006",
    "slice-9",
    "python",
    "backrun",
    "end-to-end"
  ],
  "status": "open",
  "created_at": "2026-06-18T07:15:21.757Z",
  "assigned_to_session": "019ed8af-4f02-7e42-8bab-a8df31e260a5"
}

**Master: `TODO-215e9e66` (ADR-006).** Deps: slice 8. Addresses ADR-006 acceptance (backrun end-to-end under the new topology).

**Goal.** Migrate `examples/eth_backrun_v2_v3_v4_rust.py` (~1700 lines, today driving `UniswapArbEngine()` standalone with its own `Bot` separate from `bot.from_config_file()`'s `PyBot`) to the ADR-006 topology: one `Bot` per chain, `Bot::new(chain, rpc).attach_engine(UniswapArbEngine).start()`, pools registered ONCE (via `bot.build_pool` → `PyBot`/`BotState`), engine subscribes via `register_path([(pool_id, zfo), ...])`. The dual-registration flow (slice-2's removal) is gone end-to-end; encoding reads live `Bot` state (the §17 stale-state caveat's final closure).

**Python work.**
- `self.engine = UniswapArbEngine()` (line 610) → `bot = Bot.from_config_file()` (single-chain, post-slice-8); `engine = UniswapArbEngine(py_bot=bot._py_bot)`; `bot.attach_engine(engine)`.
- Pool registration (lines ~915-985): keep `bot.build_pool(...)` / `bot.build_managed_pool(...)` (these now register once in the shared `BotState`). Remove the `engine_registry.register_v2_pool(pool)` etc. re-registration calls — replace with `engine.register_path([(pool.pool_id, zero_for_one), ...])` from the already-built Python pool's `pool_id`.
- The dispatch/encode/simulate/submit hot loop: encoding reads `pool.<state>` (via `PyLiquidityPool` → the SAME `BotState` the pump mutates) — no longer stale. Verify encoding reads fresh state post-pump-update within a block.
- The `engine_registry` wrapper class (~line 595-640): collapse; its `register_*` methods retire; `register_path` is the new entry.
- The `examples/eth_backrun_helpers.py` `encode_cmd_stream` path: unchanged mechanically; just receives fresh-state amounts.

**Tests.**
- The backrun bot's existing permutation/integration tests run green under the new topology (or are adapted if they referenced the old dual-registration seam).
- A round-trip: build a pool via `bot.build_pool`, attach it via `engine.register_path`, drive a synthetic Sync log, assert encoding reads the updated state (proves the §17 caveat is closed end-to-end).

**Acceptance.** `just test-all` green; backrun example runs end-to-end under the ADR-006 topology with ONE `Bot` per chain; the dual-registration flow is gone; encoding reads live `Bot` state (the §17 stale-state caveat is structurally closed — its long-term fix, "Rust-owned encoding," is now reachable through the same `Bot` the engine pump mutates). The standalone-Rust path (`Bot::new(chain, rpc).attach_engine(...).start()`) and the Python-driven path hit identical code. This slice is the ADR-006 overall-acceptance proof.
