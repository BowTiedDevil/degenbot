{
  "id": "31efbc18",
  "title": "Fix V4 sqrt_price_math error messages (7 tests)",
  "tags": [
    "v4",
    "sqrt-price-math",
    "error-messages"
  ],
  "status": "done",
  "created_at": "2026-06-14T19:17:56.995Z"
}

7 test failures in `tests/uniswap/v4/libraries/test_sqrt_price_math.py`.

**Root cause**: Rust `ClMathError` messages don't match V4 Python messages.

| Rust message | Expected V4 message |
|---|---|
| `InvalidPrice` | `InvalidPriceOrLiquidity` |
| `Insufficient liquidity` | `NotEnoughLiquidity` |
| `InvalidPrice` | `required: sqrt_price_x96 > 0` |
| `PriceOverflow` | (may need to check individual tests) |

**Fix**: Update Rust `ClMathError` variant messages in `rust/src/errors.rs` to match V4 Solidity error messages. V4 uses different error strings than V3 — need to parametrize or use V4-specific variants.

Alternative: Create separate V4 error messages by splitting `ClMathError` into V3/V4 variants or making the PyO3 wrapper translate messages.

**Tests**:
- `test_get_next_sqrt_price_from_output_reverts_if_output_amount_is_greater_than_the_virtual_reserves_of_currency1` — expects `NotEnoughLiquidity`, gets `Insufficient liquidity`
- `test_get_next_sqrt_price_from_output_reverts_if_price_is_zero` — expects `InvalidPriceOrLiquidity`, gets `InvalidPrice`
- `test_get_next_sqrt_price_from_output_reverts_if_liquidity_is_zero` — expects `InvalidPriceOrLiquidity`, gets `InvalidPrice`
- `test_get_next_sqrt_price_from_input_reverts_if_input_amount_overflows_the_price` — expects `PriceOverflow` ✓ (already fixed)
- `test_get_next_sqrt_price_from_output_reverts_if_output_amount_is_exactly_the_virtual_reserves_of_currency1` — expects `NotEnoughLiquidity`, gets `Insufficient liquidity`
- `test_get_next_sqrt_price_from_input_reverts_if_liquidity_is_zero` — expects `InvalidPriceOrLiquidity`, gets `InvalidPrice`
- `test_get_next_sqrt_price_from_input_reverts_if_price_is_zero` — expects `InvalidPriceOrLiquidity`, gets `InvalidPrice`
- `test_get_next_sqrt_price_from_input_returns_the_minimum_price_for_max_inputs` — likely `PriceOverflow`
