{
  "id": "0359653c",
  "title": "Update mainnet.env with new executor ABI",
  "tags": [],
  "status": "done",
  "created_at": "2026-05-27T21:42:38.923Z"
}

The `EXECUTOR_CONTRACT_ABI` in `examples/mainnet.env` is the OLD V3-only ABI (no V2 callbacks, no V4, no `bribe_bips`, no `skip_profit_check`, no `Payload.value`, no `v4_swaps`, includes `execute_packed_payloads`). Update it to match `contracts/tstore_executor_abi.json`. Also update the `EXECUTOR_CONTRACT_ADDRESS` comment to note it's the old executor and needs updating after deployment.
