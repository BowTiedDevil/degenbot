{
  "id": "8198f8ed",
  "title": "Implement Plan 099: Rust engine state observability dumps",
  "tags": [
    "mev-bot",
    "rust",
    "observability",
    "plan-099"
  ],
  "status": "closed",
  "created_at": "2026-06-16T19:20:37.747Z"
}

Implement the diagnostic state-dump work described in `plans/completed/099-rust-state-observability.md`.

Key deliverables:
- Rust `diagnostic_inspect_path(path_id, rpc_url)` on `PyUniswapArbEngine`.
- Snapshot engine state under lock, release lock, then fetch on-chain state for V2/V3/V4 hops.
- Python opt-in `STATE_DUMP_ON_REVERT` JSONL writer in `examples/eth_backrun_v2_v3_v4_rust.py`.
- Rust unit tests for snapshot and diff logic.

Prerequisite for tackling TODO-071d4791 (encoder/permutation runner correctness).

Completed slices:
- Slice 1: `diagnostic.rs` types and `UniswapEngine::diagnostic_path_state`.
- Slice 2: `PyUniswapArbEngine.diagnostic_inspect_path(path_id, rpc_url=None)` exposed.
- Slice 3: V2 `getReserves`, V3 `slot0`+`liquidity`, V4 `StateView.getSlot0`/`getLiquidity` on-chain fetches with per-field diff generation.
- Slice 4: `examples/eth_backrun_v2_v3_v4_rust.py` JSONL dump on simulation revert, deduplicated by `(path_id, solve_block)`.
- Slice 5: `just lint`, `just test-all`, and `rust/CONTEXT.md` update. Fixed pre-existing `contracts/user-guide.md` TOC link so lint is clean.

Incidental fix: corrected `pyproject.toml` `build-system.requires` from `"maturin ~= 1"` to `"maturin >= 1.0,<2.0"` so uv can build the extension.
