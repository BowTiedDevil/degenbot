{
  "id": "7b245a80",
  "title": "remove: delete dead has_unsent_results field from UniswapEngine",
  "tags": [
    "rust",
    "solver",
    "cleanup",
    "solver-review"
  ],
  "status": "complete",
  "created_at": "2026-06-16T22:42:52.968Z",
  "assigned_to_session": "019ed294-de51-7811-bc70-94e711a07006"
}

## Problem
`UniswapEngine.has_unsent_results: bool` is dead state — it is written but **never read** in any conditional. The real mechanism preventing eagerly-solved new paths from being dropped when `rebuild_and_solve_affected` replaces `self.results` is `pending_new_paths`.

## Evidence (verified — zero read sites across `rust/src/`)
- Declared: `rust/src/optimizers/uniswap_engine/mod.rs:366`
- Initialized: `mod.rs:404` (`has_unsent_results: false,`)
- Set `true`: `lifecycle.rs:52` (in `register_and_solve_path`)
- Set `false`: `result_channel.rs:119` (in `compute_diff_and_send`)
- **No conditional reads** — grep finds only the 4 references above.
- Actual mechanism: `pending_new_paths` (declared in `mod.rs`; merged into the affected set in `rebuild_and_solve_affected` at `solver_dispatch.rs:67`).

## Scope
Remove the field and its assignments:
- struct field `has_unsent_results: bool,` → `mod.rs:366`
- init `has_unsent_results: false,` → `mod.rs:404`
- `self.has_unsent_results = true;` → `lifecycle.rs:52`
- `self.has_unsent_results = false;` → `result_channel.rs:119`
- Any doc comments referencing it (check the field comment).

## Acceptance
- [ ] `rg -n "has_unsent_results" rust/src/` returns zero hits.
- [ ] `just test-rust` passes (Red/Green: confirm a regression test isn't needed since no behavior depends on it, but re-run the `uniswap_engine` unit tests module to be safe).
- [ ] `just lint-rust` (clippy, pedantic) clean — no new warnings.
- [ ] Confirm `pending_new_paths` still carries the "don't drop eagerly solved paths on next rebuild" behavior — no regression in `register_and_solve_path` / `rebuild_and_solve_affected`.

## Notes
- **Verify intent before deleting**: this field was *probably* meant to gate an early `send_result_batch` after `register_and_solve_path`. If a follow-up intended to wire it up as such, flag and escalate rather than deleting — but as-is it is unreachable dead state.
- Per AGENTS.md §Rust: **do NOT manually rebuild the extension** — running tests triggers the maturin rebuild on import.
- Commit: `remove(rust): delete dead has_unsent_results field`.
