{
  "id": "a42cd3d7",
  "title": "Slice 6: DexIdentity + DEX presets in Rust core",
  "tags": [
    "polars-three-layer",
    "adr-005",
    "dex-identity",
    "rust",
    "standalone-core",
    "slice-6"
  ],
  "status": "done",
  "created_at": "2026-06-17T19:26:13.896Z"
}

**Slice 6 of the Polars three-layer migration (ADR-005).** Master: `TODO-7e24d695`. Deps: none. **STATUS: DONE.** Commit `31ba43bc` — "DexIdentity + DEX presets in Rust core (ADR-005 slice 6)". Red/Green TDD: 14 Rust unit tests (in `dex_identity.rs` `#[cfg(test)]`) + 9 Python seam tests (`TestDexIdentityPresets` in `test_py_bot.py`). New pure-Rust (zero-pyo3, acceptance-verified) `dex_identity.rs` with `DexIdentity` frozen struct + `DexVariant`/`ReservesAbi` enums + 8 `pub const` presets carrying verified canonical-chain values cross-checked against the Python ClassVars (`UNISWAP_V2_MAINNET_POOL_INIT_HASH`, Pancake `Fraction(25,10000)`+`RESERVES_STRUCT_TYPES`, Aerodrome `FEE_DENOMINATOR=10000`, `CAMELOT_ARBITRUM_POOL_INIT_HASH`). Python seam `py_dex_identity.rs`: `#[pyclass(frozen, skip_from_py_object)] PyDexIdentity` + `#[pyfunction] dex_identity(variant)` (OnceLock-cached, case-insensitive, returns None for unknown). pyi + `__all__` wired. `fee_tokenN: (u64,u64)` = `(gamma_numer, fee_denom)` matches `V2PoolState` so slice 7 can feed `register_v2_pool` directly — bakes the slice-5 convention in by construction. Inert addition: zero regressions (344 Python tests + Rust 481 = 467+14 new). Slice 7 (DEX subclass collapse) is the first real consumer. (Amend note: initial commit missed the 2 new files via `git add -u` — amended to include them; the committed state is self-contained green.)
