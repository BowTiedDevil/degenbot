{
  "id": "c538ee36",
  "title": "Remove `running` guard from Rust engine pool/path registration",
  "tags": [
    "rust",
    "engine"
  ],
  "status": "complete",
  "created_at": "2026-06-03T16:58:56.239Z",
  "assigned_to_session": "019e8e35-11b6-723a-8249-3c43e7ea2340"
}

COMPLETED. Changes:

Rust:
- Removed `running` field, `start()`, `is_running()` from V2BlockEngine, V3BlockEngine, V4BlockEngine, UniswapEngine
- Removed `assert!(!self.running)` from `register_pool` and `register_path` in all four engines
- Removed `freeze()` and `initial_solve()` from PyO3 `UniswapArbEngine` wrapper
- Removed `freeze()` and `is_running()` from V2/V3 PyO3 wrappers
- Removed `initial_solve()` from V4BlockEngine
- Added `solve_all_paths(block_number)` to UniswapEngine (replaces initial_solve for seed batch)
- Updated tests: panic→succeeds, removed engine.start() calls, added register_after_process_logs tests

Python:
- Updated test_uniswap_arb_engine.py: replaced freeze+initial_solve with solve_all_paths
- Updated test_v2_block_engine.py: replaced freeze tests with always-on tests
- Updated test_v3_block_engine.py: replaced freeze tests with always-on tests
- Updated test_engine_v3v3_vs_brent.py: replaced freeze+initial_solve with solve_all_paths
