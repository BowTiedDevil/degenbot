{
  "id": "bb1ac525",
  "title": "Add V4-hybrid path encoding to CmdExecutorComposer",
  "tags": [
    "arbitrage",
    "cmd-executor",
    "v4"
  ],
  "status": "closed",
  "created_at": "2026-06-01T22:40:32.794Z"
}

## Completed
- [x] Implement `_encode_cmd_v4_v4()` — both swaps inside V4_UNLOCK, take profit + settle
- [x] Implement `_encode_cmd_v4_v3()` — V4 swap + take forward + V3 auto-pay + settle WETH
- [x] Implement `_encode_cmd_v3_v4()` — V3 swap (auto-pay) + sync/transfer/settle + V4 unlock with swap + take_delta
- [x] Implement `_encode_cmd_v4_v2()` — V4 swap + take to V2 directly + V2_SWAP_CALC + settle
- [x] Implement `_encode_cmd_v2_v4()` — V2 flash borrow + sync/transfer/settle + V4 unlock with swap + take
- [x] Route all V4-hybrid path types in `encode_cmd_stream()`
