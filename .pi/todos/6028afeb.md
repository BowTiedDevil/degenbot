{
  "id": "6028afeb",
  "title": "Refactor Python bot for rolling-start startup sequence",
  "tags": [
    "python",
    "bot"
  ],
  "status": "complete",
  "created_at": "2026-06-03T16:59:21.454Z",
  "assigned_to_session": "019e8e35-11b6-723a-8249-3c43e7ea2340"
}

COMPLETED. Changes:

examples/eth_backrun_v2_v3_v4_rust.py:
- Replaced `engine.freeze()` + `engine.initial_solve()` with `engine.solve_all_paths()`
- Added `_pump_running: bool = False` field to `EngineRegistry` with thread-safety note
- Updated `EngineRegistry.register_path()` to branch on `_pump_running`: if running, uses `register_and_solve_path` (eager solving); otherwise uses `register_path` (batch solve later)
- Replaced `engine.start(node_ws)` with two-phase lifecycle: `engine.subscribe(node_ws)` → backfill → `engine.resume()`
- `subscribe()` called immediately at startup, before path loading and backfill
- Backfill target comes from `subscribe()` return value, not a separate RPC call
- After backfill, `_pump_running` set to True and `resume()` called
- New startup sequence: subscribe → observe first block → build_paths → backfill → verify → resume → main loop
- All Python key dicts remain plain dicts (single asyncio event loop) with explicit note
