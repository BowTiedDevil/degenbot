{
  "id": "e9ea3c51",
  "title": "Fix V4 full_math + swap_math error messages (3 tests)",
  "tags": [
    "v4",
    "full-math",
    "swap-math",
    "error-messages"
  ],
  "status": "done",
  "created_at": "2026-06-14T19:18:01.145Z"
}

3 test failures.

**Root cause 1 (V4 full_math)**: `test_fuzz_mul_div_reverts_with_zero_denominator` and `test_fuzz_mul_div_rounding_up_reverts_with_zero_denominator` expect `required: denominator > 0` but Rust raises `DIVISION BY ZERO`. Also `test_mul_div_reverts_with_overflowing_numerator_and_zero_denominator` expects same.

**Root cause 2 (V4 swap_math)**: `test_fuzz_compute_swap_step` — may be related to keyword arg forwarding or overflow in `compute_swap_step_v4`.

**Fix**: 
- For V4 full_math: the `_wrap` function in V4 `full_math.py` already converts ValueError→EVMRevertError, but the error message from Rust is `DIVISION BY ZERO` while V4 expects `required: denominator > 0`. Need to add message translation in the V4 wrapper.
- For V4 swap_math: check the exact error in the test output.
