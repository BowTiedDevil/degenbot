{
  "id": "9ac909c4",
  "title": "Slice 9: V4 companion (library path) — UniswapV4Pool over PyLiquidityPool",
  "tags": [
    "polars-three-layer",
    "adr-005",
    "v4",
    "python",
    "rust",
    "slice-9"
  ],
  "status": "open",
  "created_at": "2026-06-17T19:26:40.022Z"
}

**Slice 9 of the Polars three-layer migration (ADR-005).** Master: `TODO-7e24d695`. Deps: slice 8 (V3 companion + V4 shared CL state machinery).

**Goal.** Make `UniswapV4Pool` a companion over `PyLiquidityPool` (V4 variant); state delegates; `PyBot` GAINS V4 register/update methods (currently only `UniswapArbEngine` registers V4). Library/non-engine V4 use goes through `PyBot`. Hook filtering (0xCC mask) + dynamic-fee exclusion (`0x100000`) stay Python (per AGENTS.md: "V4 pools with amount-modifying hooks are rejected in Python before `register_v4_pool`").

**Rust.**
- ADD `PyBot.register_v4_pool`/`update_v4_pool` (Rust `Bot::register_v4_pool` + `update_v4_pool` exist per CONTEXT.md — V4 state lives in `Bot`). Mirror the V3 PyO3 shape.
- ADD V4 read getters to `PyLiquidityPool`: `sqrt_price_x96`, `tick`, `liquidity`, `pool_key` (the `V4PoolKey`: currency0/currency1/fee/tick_spacing/hooks). Dispatch by variant on `Bot::get_v4_pool`.

**Python class.** `UniswapV4Pool` (`src/degenbot/uniswap/v4_liquidity_pool.py`, 1169 lines): `__init__` gains `self._py_pool`; state reads delegate; `external_update` (Swap/ModifyLiquidity decoders) → `PyBot.update_v4_pool`; calc → `self._py_pool.calculate_tokens_out` (Rust `v4_simulate_swap`). Hook filtering / dynamic-fee rejection stays Python (runs before `PyBot.register_v4_pool`). Keep `PublisherMixin`/`PoolPickleMixin`/`UniswapV4PoolCalc`.

**Builders.** `V4PoolBuilder` constructs via `PyBot.register_v4_pool` + tick snapshot → wraps. `build_managed_pool(address, pool_id)` → companion. `V4BuilderBase` static helpers feed register params.

**Tests.** V4 pool tests (calc outputs, Swap/ModifyLiquidity application); hook-filter rejection tests; `test_py_bot.py` V4 tests (new).

**Consistency at boundary.** V4 companion works for library use (non-engine). **Engine path still separate** (`UniswapArbEngine`'s `Bot`) — engine-managed V4 pools' companion reads are STALE until slice 10 (documented in the companion docstring; same dual-state baseline as V2/V3). All V4 tests pass; no deadlock surface (the two `Bot` instances are independent until slice 10).

**Acceptance.** `cargo`/`ruff`/`ty` green; V4 library-path tests + hook-filter rejection tests pass; `PyBot.register_v4_pool` smoke test; tick-data + pool_key reads match.

**NOTE.** The engine-managed V4 case (companion reads live engine-updated state) is slice 10's payoff.
