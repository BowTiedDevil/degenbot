{
  "id": "87bb26eb",
  "title": "Add expected_balance parameter to execute() call",
  "tags": [
    "encoding",
    "profit-check",
    "ABI",
    "P0"
  ],
  "status": "complete",
  "created_at": "2026-06-13T23:08:01.400Z"
}

## Summary
DONE: expected_balance parameter added to execute() call.

### Changes Applied
- Updated EXECUTOR_ABI in eth_backrun_v2_v3_v4_rust.py to include `expected_balance: uint256`
- Changed `execute(bytes)` selector to `execute(bytes,uint256)` in simulate_one()
- Added `pack_expected_balance()` helper in cmd_stream.py: format is `(value << 8) | check_mode`
- Currently using check_mode=0 (skip on-chain check) with expected_balance=0 — operator verifies profitability off-chain via pre/post balance reads
- The ABI file (cmd_executor_abi.json) already has both overloads

### Future Work
- For production: use check_mode=1 (WETH+ETH) or check_mode=2 (ERC6909) with pre-tx balance fetched per-dispatch
