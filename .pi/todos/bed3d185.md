{
  "id": "bed3d185",
  "title": "Add `register_and_solve_path` with eager solving",
  "tags": [
    "rust",
    "engine"
  ],
  "status": "closed",
  "created_at": "2026-06-03T16:58:59.566Z"
}

COMPLETED. Changes:

Rust:
- Added `pending_new_paths: HashSet<u64>` field to `UniswapEngine`
- Added `register_and_solve_path()` method: registers path, resolves, solves, appends to results, adds to pending set
- Updated `rebuild_and_solve_affected()` to merge `pending_new_paths` into `affected_path_ids` and clear the set
- Exposed `register_and_solve_path()` via PyO3 (same signature as `register_path`)
- Added 3 Rust tests: eagerly_solves, pending_survive_rebuild, pure_v2_path (existing)

Python:
- Added `TestRegisterAndSolvePath` class with 3 tests: eager_solve_produces_results, eager_solve_survives_process_logs, register_and_solve_after_process_logs
