{
  "id": "95e9a50c",
  "title": "Implement Mint/Burn event buffer for unregistered pools",
  "tags": [
    "rust",
    "engine"
  ],
  "status": "complete",
  "created_at": "2026-06-03T16:59:04.435Z",
  "assigned_to_session": "019e8e35-11b6-723a-8249-3c43e7ea2340"
}

COMPLETED. Changes:

Rust (V3BlockEngine):
- Added `BufferedV3LiquidityUpdate` struct: tick_lower, tick_upper, liquidity_delta (i128), block_number
- Added `liquidity_event_buffer: HashMap&lt;Address, Vec&lt;BufferedV3LiquidityUpdate&gt;&gt;` field
- `apply_liquidity_update()`: when pool not registered, buffers the update instead of silently returning
- `register_pool()`: after insertion, applies any buffered events and removes the buffer entry
- Added 2 tests: buffered mint applied on registration, multiple buffered events applied in order

Rust (V4BlockEngine):
- Added `BufferedV4LiquidityUpdate` struct: tick_lower, tick_upper, liquidity_delta (I256), block_number
- Added `liquidity_event_buffer: HashMap&lt;(Address, PoolId), Vec&lt;BufferedV4LiquidityUpdate&gt;&gt;` field
- `apply_liquidity_update()`: when pool not registered, buffers the update instead of silently returning
- `register_pool()`: after insertion, applies buffered events to both forward and reverse orientations
- Added 1 test: buffered ModifyLiquidity applied on registration
