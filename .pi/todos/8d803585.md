{
  "id": "8d803585",
  "title": "Slice 5: V2 companion — calc delegation to PyLiquidityPool",
  "tags": [
    "polars-three-layer",
    "adr-005",
    "v2",
    "calc",
    "slice-5"
  ],
  "status": "done",
  "created_at": "2026-06-17T19:26:13.896Z"
}

**Slice 5 of the Polars three-layer migration (ADR-005).** Master: `TODO-7e24d695`. Deps: slice 4. **STATUS: DONE.** Commit `b6bdeb16` — "delegate V2 calc to PyLiquidityPool". Red/Green TDD: 9 delegation-detection tests (`TestV2CalcDelegation`; `_DelegateSpy` wrapper records `calculate_tokens_out/in` calls on the handle). The constant-product calc on the NON-override path delegates; the override path stays Python (so `simulate_*` can hold one snapshot Python-side per slice-4's delta-consistency fix). **Two latent bugs surfaced + fixed:** (1) register-time fee-convention mismatch — slice-4's `make_v2_pool`/`V2PoolBuilder`/`CamelotBuilder` registered `gamma_numer = fee.numerator` (fee semantics); Rust's `IntHopState` uses the retained post-fee fraction (`997` for `0.3%`); fix: emit the complement (`fee.denom - fee.numerator`). (2) slice-4 missed `0xt0`/`0xt1`/`0xt2`/`0xtokenA`/`0xtokenB`/`0xtokenC` short non-hex token sentinels in `tests/arbitrage/test_path/`; replaced with valid 40-hex → 69 tests re-enabled. Anvil-verified Camelot fork (`getAmountOut` parity) + override fork tests pass. Calc delegation is now the V2 shape: state via `snapshot()` + calc via `calculate_tokens_out/in` + sync_reserves/discard/restore via the handle, EXCEPT the override path (Python, for `simulate_*`) + Camelot/Aerodrome stable strategies (ADR-005 B-ii). Next per master: slice 6 (`DexIdentity` in Rust core, `TODO-a42cd3d7`) → slice 7 (DEX subclass collapse).
