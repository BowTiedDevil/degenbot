## Supervisor adversarial review — KMTE5I (PASSED after one fix round)

## Verified
- Move-only split: 816 passed / 3 ignored before AND after; per-file line counts all <=1457
- just verify-build-fresh exit 0; fmt gates green after one delegation round

## Critical lesson (process gap closed)
verify-build-fresh fingerprints ONLY degenbot-python sources. The first split iteration broke the maturin extension build (E0433: missing feature-gated 'use super::Arc;' in run_loop.rs) while cargo test -p degenbot-bot stayed green (default features don't compile that line). Delegated fix added the hotpath-gated import matching lifecycle.rs conventions; full feature set 'cargo check -p degenbot-bot --features hotpath,hotpath-prometheus,allocator-ctrl' + end-to-end maturin rebuild now green.

## New review rule for Rust-crate changes
Any change under a rust/crates non-python crate must additionally be proven against the exact extension feature set (hotpath, hotpath-prometheus, allocator-ctrl) before commit. uv sync --reinstall-package degenbot succeeding end-to-end is the proof.
