"""Example scripts: single-file runnable demonstrations of the degenbot API.

The settlement-arbitrage MEV bot examples here are intentionally self-contained scripts
(``python examples/eth_backrun_v2_v3_v4_rust.py``) rather than library code —
they trade strict library-style lint for readability and a runnable narrative.
They are not part of the ``src/`` lint gate; the ``examples/*`` per-file-ignore
block in ``pyproject.toml`` documents the demonstration-appropriate relaxations.
"""
